# Discord Messages Dump Configuration
# Copy this file to .env and fill in your values

# Your Discord user token (required)
# This is NOT a bot token. To obtain it:
# 1. Open Discord in your web browser or desktop app
# 2. Press Ctrl+Shift+I (or Cmd+Option+I on macOS) to open developer tools
# 3. Go to the 'Network' tab
# 4. Make any request on Discord, such as changing channels
# 5. Find a request in the Network tab
# 6. Look for the 'authorization' header in the request headers
# 7. The value of that header is your user token
DISCORD_TOKEN="your_discord_token_here"

# The ID of the Discord channel to fetch messages from (required)
# To obtain the channel ID:
# 1. Enable developer mode in Discord settings (User Settings -> Advanced -> Developer Mode)
# 2. Right-click the channel and select 'Copy ID'
DISCORD_CHANNEL_ID="your_channel_id_here"

# Optional: Default output format (text, json, csv, markdown)
# If not specified, defaults to text
# FORMAT="text"

# Optional: Default maximum number of messages to retrieve
# If not specified, defaults to 100
# MESSAGE_LIMIT=100

# Optional: Default output file path
# If not specified, a file dialog will open (unless --no-gui is used)
# OUTPUT_FILE="discord_messages.txt"

# Optional: Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# If not specified, defaults to INFO
# LOG_LEVEL="INFO"
